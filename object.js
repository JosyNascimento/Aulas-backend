const coder ={
    nome: 'tuma 63505',
    aulas: 'Terças e quintas',
    professora: 'Alessandra',
};
console.log(coder);

const chaveValor = object.entries(coder);
console.log(chavValor[0][1]);

const keys =object.keys (coder);
console.log(keys)